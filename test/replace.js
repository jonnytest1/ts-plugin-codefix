

const exampleFor = `


    for (const t of test) {
        debugger
    }`

const identifierMAtch =


const relpalced = exampleFor.replace(/for.*?{/, (...args) => {
    const groups = args.at(-1)


    return `for (let i=0;i<${list}.length;i++){
        const 
    `
    debugger
})
debugger